name: Review

on:
  pull_request:
    types:
      - opened
      - edited
  workflow_dispatch:

jobs:
  assignee:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: >-
      github.event.pull_request.user.login != ${{ vars.MEX_BOT_USER }} &&
      github.event.pull_request.user.login != 'dependabot[bot]'
    steps:
      - name: Add assignee
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr view ${{ github.event.pull_request.html_url }}
          echo user.login ${{ github.event.pull_request.user.login }}
          echo assignee ${{ github.event.pull_request.assignee.login }}
          if [[ "${{ github.event.pull_request.user.login }}" -eq "${{ github.event.pull_request.assignee.login }}" ]]; then
            echo samesame
          fi
          echo done
